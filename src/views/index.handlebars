<div>
    {{#each products}}
    <img src={{this.thumbnails}} alt="">
        <p>{{this.title}}</p>
        <p>{{this.description}}</p>
        <p>{{this.price}}</p>
    {{/each}}
</div>
<button id="button">Siguiente Pagina</button>
<script src="js/products.js"></script>
